# Web Framework & API
Django==5.1.4
djangorestframework==3.15.2
django-cors-headers==4.6.0

# Authentication (Likely needed for your API)
djangorestframework-simplejwt==5.3.1

# Production Server
gunicorn==23.0.0
whitenoise==6.8.2

# Database (Supabase/PostgreSQL)
psycopg2-binary==2.9.10

# Supabase Client (For Supabase functions, not just the DB)
supabase==2.15.3

# Local Development (Loads .env file)
python-dotenv==1.0.1

# HTTP Requests (NEW)
requests

# PDF Processing (NEW)
pdfplumber